# Operation Model (KAOS)

<!-- Leaf goals operationalized into operations. Semi-formal: conditions in
     natural language, not temporal logic. -->

## Operations
<!-- Each operation operationalizing a leaf goal. (Agent ownership lives in the
     responsibility model; joined via the traceability matrix — do not restate it here.)
     ### Operation: <name>
     - Pre-condition: <what must hold before>
     - Post-condition: <what holds after>
     - Trigger: <what initiates it> -->

## Scenarios
<!-- At least one nominal scenario per operation, plus one per resolved obstacle whose
     resolution adds runtime behavior. For weaken-goal / out-of-software substitute-agent
     resolutions, note "no scenario applies" instead of inventing one.
     ### Scenario: <name>
     - Given <context>
     - When <action/operation>
     - Then <outcome; reference the satisfied goal by name, do not restate its definition> -->
